# Tests Directory

Primary automated tests live in [`backend/tests`](/c:/Users/Yass0/OneDrive/Desktop/TASK14/backend/tests).

Run:
- `set PYTHONPATH=backend && pytest backend/tests -q` (Windows)
- `PYTHONPATH=backend pytest backend/tests -q` (Linux/macOS)
